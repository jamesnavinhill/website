// Vitest base configuration (no Storybook integration)

import { defineConfig } from "vitest/config";

// no-op

export default defineConfig({
  test: {
      // Standard project config (no Storybook integration)
  },
});
